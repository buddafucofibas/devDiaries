extends ../layout

block content
    section.landing_wrapper.flex
        .landing_main.flex
            .wrapper_backing
            .wrapper.flex
                h2.home_heading devDiaries
                .link_wrapper
                    div.home_nav
                        a(href='/login' ) Login
                    div.home_nav
                        a(href='/register' ) Register