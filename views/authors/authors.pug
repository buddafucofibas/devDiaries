extends ../layout

block content
    h1 devDiaries Authors

    .authors
        each author in authors
            a(href='/authors/' + author._id)
                .author_card
                    .img
                        img(src='https://source.unsplash.com/featured/?headshot')
                    .name
                        p #{author.firstName} #{author.lastName}
                    .bio
                        h3 BIO
                        p Lorem, ipsum dolor sit amet consectetur adipisicing elit. Nihil, nemo.

    div
        button
            a(href='/members') Members Area

    div
        button
            a(href='/') Home